apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-platform
  namespace: flux-system
spec:
  serviceAccountName: kustomize-controller
  interval: 10m
  prune: true
  wait: true

  path: ./clusters/cluster-02-rke2/platform
  sourceRef:
    kind: GitRepository
    name: flux-system
